global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "pgagroal"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["host.docker.internal:5001"] # Assuming pgagroal runs on host port 5001
