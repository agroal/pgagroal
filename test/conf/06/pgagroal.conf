[pgagroal]
host = localhost
port = 2345

log_type = file
log_level = debug5
log_path = test.log

max_connections = 200
idle_timeout = 600
validation = off
unix_socket_dir = /tmp/
pipeline = session
ev_backend = epoll

[primary]
host = localhost
port = 5432