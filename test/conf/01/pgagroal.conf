[pgagroal]
host = localhost
port = 2345

log_type = file
log_level = debug5
log_path = test.log

max_connections = 100
idle_timeout = 600
validation = off
unix_socket_dir = /tmp/
ev_backend = io_uring
pipeline = transaction
allow_unknown_users = false

[primary]
host = localhost
port = 5432