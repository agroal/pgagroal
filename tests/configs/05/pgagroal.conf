[pgagroal]
host = *
port = 2345

log_type = file
log_level = debug5
log_path = test.log

max_connections = 8
idle_timeout = 600
validation = off
unix_socket_dir = /tmp/
ev_backend = io_uring
pipeline = 'transaction'

[primary]
host = localhost
port = 5432
